---
"@urban/gitai": patch
---

Add Ralph task tracking files, boot the Bun-backed `gitai commit` CLI with the approved zero-or-one instruction grammar, introduce schema-backed commit contracts, service seams, and stderr-aware terminal rendering coverage, implement staged Git snapshot loading with temp-repo coverage for nested-directory resolution plus typed not-a-repo and no-staged-changes failures, and add approved-message commit creation with staged fingerprint revalidation.

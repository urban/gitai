---
"@urban/gitai": patch
---

Ship the first `gitai commit` slice with a role-first Effect CLI layout, `src/cli.ts` as the runtime entrypoint, Effect platform child-process and file-system abstractions for git + temp-file work, Bun runtime execution, thin command wiring, split domain-error-service modules, Vitest + `@effect/vitest` test suites with Effect-native test layers, colocated tests, and PATH-style nested-repo execution coverage.
